---
spec:
  inputs:
    module_dir:
      default: '$CI_PROJECT_DIR'
      description: >-
        Directory containing the target Terraform module. Specify a subdirectory for monorepo layouts.

    module_name:
      default: '$CI_PROJECT_NAME'
      description: 'Terraform Module Registry module name.'
      regex: '^\$[A-Z_][A-Z0-9_]*$|^[a-zA-Z0-9_-]+$'
    module_system:
      default: 'local'
      description: 'Terraform Module Registry system (provider) identifier, e.g. gitlab.'
    curl_image:
      default: 'curlimages/curl:8.10.1'
      description: 'curl image for the registry upload job.'
    tag_prefix:
      default: '$[[ inputs.module_name ]]-'
      description: >-
        Literal tag prefix required for module publication, matched against CI_COMMIT_TAG.
        Defaults to the auto-tag module_name prefix. Set to '' for un-prefixed repository tags
        to prevent cross-module job execution.
---
# Packages module_dir and uploads to the GitLab Terraform Module Registry on tag push.
# Replicates upload behavior from Terraform/Module-Base.gitlab-ci.yml without stage dependencies.
# Embeds module_name in the job identifier to prevent definition collisions.
deploy:terraform-module-$[[ inputs.module_name ]]:
  stage: deploy
  image: '$[[ inputs.curl_image ]]'
  interruptible: false
  rules:
    - if: '$CI_COMMIT_TAG =~ /^$[[ inputs.tag_prefix ]]/'
  script:
    - MODULE_NAME=$(echo "$[[ inputs.module_name ]]" | tr " _" -)
    - TAR_FILENAME="/tmp/${MODULE_NAME}-$[[ inputs.module_system ]]-${CI_COMMIT_TAG}.tgz"
    - |
      UPLOAD_URL="${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/terraform/modules/${MODULE_NAME}\
      /$[[ inputs.module_system ]]/${CI_COMMIT_TAG}/file"
    - |
      tar -vczf "${TAR_FILENAME}" -C "$[[ inputs.module_dir ]]" \
      --exclude=./.git --exclude=./.terraform --exclude='*.tfstate*' --exclude='*.tfvars' .
    - |
      curl --fail-with-body --location --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
      --upload-file "${TAR_FILENAME}" "${UPLOAD_URL}"

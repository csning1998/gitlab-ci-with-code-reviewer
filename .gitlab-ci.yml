---
stages:
  - format
  - build
  - lint
  - test
  - security
  - review
  - release

include:
  # reviewer_image references the :edge tag image built by this pipeline to support pre-merge self-review.
  - local: templates/core.yml
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    inputs:
      reviewer_image: "registry.gitlab.com/csning1998-lab/gitlab-ci-with-code-reviewer/reviewer:edge"
      review_models:
        - "claude-sonnet-5"
        - "gemini-3.7-flash"
      enable_sonarqube: true
      sonar_scanner_tags: ["sonarqube-network"]
      sonar_go_coverage_path: "tools/ci/coverage.out"
  - local: templates/lang-go.yml
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    inputs:
      go_globs:
        - "tools/**/*.go"
        - "tools/**/go.mod"
        - "tools/**/go.sum"
        - "templates/lang-go.yml"
        - ".gitlab/golangci.yml"
  - local: templates/iac-terraform.yml
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    inputs:
      checkov_skip: "CKV_GIT_1,CKV_GLB_1,CKV_GLB_3,CKV_GLB_4,CKV_TF_1"
  - local: templates/auto-tag.yml
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    inputs:
      reviewer_image: "$CI_REGISTRY_IMAGE/reviewer:edge"
      stage: release
  - local: .gitlab/ci/verify-jvm.yml
  - local: .gitlab/ci/verify-c-cpp.yml
  - local: .gitlab/ci/verify-csharp.yml
  - local: .gitlab/ci/verify-rust.yml

# Merge request events publish to the :edge container tag, whereas semver git tags produce versioned release images.
build:reviewer-image:
  stage: build
  image: quay.io/buildah/stable:v1.43.0
  needs: []
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - if: "$CI_COMMIT_TAG"
  variables:
    STORAGE_DRIVER: vfs
    IMAGE: "$CI_REGISTRY_IMAGE/reviewer"
  script:
    - buildah login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - buildah build --build-arg CI_JOB_TOKEN="${CI_JOB_TOKEN}" -t "$IMAGE:build" -f tools/ci/Dockerfile tools/ci
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        buildah tag "$IMAGE:build" "$IMAGE:$CI_COMMIT_TAG"
        buildah push "$IMAGE:$CI_COMMIT_TAG"
      else
        buildah tag "$IMAGE:build" "$IMAGE:edge"
        buildah push "$IMAGE:edge"
      fi

security:trivy-image:
  stage: security
  image:
    name: docker.io/aquasec/trivy:0.72.0
    entrypoint: [""]
  needs:
    - job: build:reviewer-image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - if: "$CI_COMMIT_TAG"
  script:
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        IMAGE_TAG="$CI_COMMIT_TAG"
      else
        IMAGE_TAG="edge"
      fi
    - trivy image --exit-code 1 --severity CRITICAL,HIGH "$CI_REGISTRY_IMAGE/reviewer:$IMAGE_TAG"

publish-catalog:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:v0.24.0
  needs:
    - job: build:reviewer-image
    - job: security:trivy-image
  rules:
    - if: "$CI_COMMIT_TAG"
  script:
    - echo "Publishing templates/ components for $CI_COMMIT_TAG to the CI/CD Catalog."
  release:
    tag_name: "$CI_COMMIT_TAG"
    description: "CI template release $CI_COMMIT_TAG"

# Will be purged after fully implemented WIF.
# test:adhoc-wif-token-exchange:
#   stage: test
#   image: badouralix/curl-jq:latest
#   id_tokens:
#     GITLAB_OIDC_TOKEN:
#       aud: "https://gitlab.com"
#   rules:
#     - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
#     - if: '$CI_PIPELINE_SOURCE == "push"'
#   script:
#     - |
#       VAULT_ADDR="https://172.16.0.1:8200"
#       CURL_TLS_ARGS=""
#       if [ -n "${BASTION_VAULT_CA_B64}" ]; then
#         echo "${BASTION_VAULT_CA_B64}" | base64 -d > /tmp/vault-ca.pem
#         CURL_TLS_ARGS="--cacert /tmp/vault-ca.pem"
#         echo "Using strict TLS verification with BASTION_VAULT_CA_B64."
#       else
#         echo "Warning: BASTION_VAULT_CA_B64 is not set in CI/CD variables; falling back to -k for adhoc test."
#         CURL_TLS_ARGS="-k"
#       fi
#       echo "1. Exchanging GitLab SaaS OIDC JWT for temporary Vault Token..."
#       LOGIN_RESP=$(curl ${CURL_TLS_ARGS} --silent --show-error --fail \
#         --request POST \
#         --data "{\"jwt\": \"${GITLAB_OIDC_TOKEN}\", \"role\": \"ci-code-reviewer\"}" \
#         "${VAULT_ADDR}/v1/auth/gitlab-saas-ci-job-jwt-provider/login")
#       CLIENT_TOKEN=$(echo "${LOGIN_RESP}" | jq -r '.auth.client_token // empty')
#       if [ -z "${CLIENT_TOKEN}" ]; then
#         echo "Error: Vault login did not yield a valid client_token."
#         exit 1
#       fi
#       echo "Successfully obtained Vault token with TTL: $(echo "${LOGIN_RESP}" | jq -r '.auth.lease_duration')s"
#       echo "2. Reading CI test credential using exchanged Vault Token..."
#       SECRET_RESP=$(curl ${CURL_TLS_ARGS} --silent --show-error --fail \
#         --header "X-Vault-Token: ${CLIENT_TOKEN}" \
#         "${VAULT_ADDR}/v1/secret/data/parent-group-governance/ci/test-credential")
#       API_KEY=$(echo "${SECRET_RESP}" | jq -r '.data.data.api_key // empty')
#       if [ -z "${API_KEY}" ]; then
#         echo "Error: Failed to retrieve secret field api_key."
#         exit 1
#       fi
#       echo "Secret retrieval verified successfully (field present, length: ${#API_KEY})."
